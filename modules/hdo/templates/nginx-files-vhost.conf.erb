#
# This file is managed by Puppet. Local edits will be lost.
#

server {
  listen      80;
  server_name files.holderdeord.no;
  root        <%= @files_root %>;
  autoindex   off;

  location /pdf/alternative-statsbudsjett {
    autoindex on;
  }

  location /pdf/retriever/utbrytere {
    autoindex on;
  }

  rewrite ^/hal-browser/?$ /hal-browser/browser.html last;
}
