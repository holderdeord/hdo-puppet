#!/bin/bash

output=$(/sbin/status statsd)
echo $output | grep 'running' > /dev/null 2>&1

retcode=$?

if [ $retcode -gt 0 ]
then
  echo -n "statsd: FAILED "
  exit 2
else
  echo -n "statsd: OK "
  exit 0
fi
