#
# This file is managed by Puppet. Local edits will be lost.
#

# nginx status
server {
  listen 80;
  server_name localhost <%= scope.lookupvar('::fqdn') %>;
  location /nginx_status {
    stub_status on;
    access_log off;
    allow 127.0.0.1;
    deny all;
  }
}
